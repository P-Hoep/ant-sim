# Tasks

## Deployment — Cloudflare Pages
- [x] Add `wrangler.toml` (project `leafcutter-colony`, output `./dist`)
- [x] Add `npm run deploy` script
- [x] Log in to Cloudflare (`wrangler login`) or set `CLOUDFLARE_API_TOKEN`
- [x] First deploy: `npm run deploy` — live at https://leafcutter-colony.pages.dev
- [ ] Optional: connect GitHub repo in Cloudflare dashboard for auto-deploys (build `npm run build`, output `dist`)
